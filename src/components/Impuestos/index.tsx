import React from 'react';
import { LottieBase } from '../Base';

export interface LottieImpuestosProps {}
export const LottieImpuestos = ({}: LottieImpuestosProps) => {
    return (
        <>
            <LottieBase img={'lottieImpuestos'} />
        </>
    );
};
