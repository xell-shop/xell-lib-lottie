import React from 'react'
import { LottieBase } from '../Base';

export interface LottieMetasVentasProps {}
export const LottieMetasVentas = ({}: LottieMetasVentasProps) => {
    return (
        <>
            <LottieBase img={'lottieMetasVentas'} />
        </>
    );
};
