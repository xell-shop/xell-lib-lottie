import React from 'react';
import { LottieBase } from '../Base';

export interface LottieConfigProps {}
export const LottieConfig = ({}: LottieConfigProps) => {
    return (
        <>
            <LottieBase img={'lottieConfig'} />
        </>
    );
};
