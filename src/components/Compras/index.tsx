import React from 'react'
import { LottieBase } from '../Base';

export interface LottieComprasProps {}
export const LottieCompras = ({}: LottieComprasProps) => {
    return (
        <>
            <LottieBase img={'lottieCompras'} />
        </>
    );
};
