import React from 'react';
import { LottieBase } from '../Base';

export interface LottieCategoryProps {}
export const LottieCategory = ({}: LottieCategoryProps) => {
    return (
        <>
            <LottieBase img={'lottieCategory'} />
        </>
    );
};
