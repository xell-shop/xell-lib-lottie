import React from 'react';
import { LottieBase } from '../Base';

export interface LottieConectarFacturadorProps {}
export const LottieConectarFacturador = ({}: LottieConectarFacturadorProps) => {
    return (
        <>
            <LottieBase img={'lottieConectarFacturador'} />
        </>
    );
};
