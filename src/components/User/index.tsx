import React from 'react'
import { LottieBase } from '../Base';

export interface LottieUserProps {}
export const LottieUser = ({}: LottieUserProps) => {
    return (
        <>
            <LottieBase img={'lottieUser'} />
        </>
    );
};
