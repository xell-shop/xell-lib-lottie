import React from 'react';
import { LottieBase } from '../Base';

export interface LottieCanastaProps {}
export const LottieCanasta = ({}: LottieCanastaProps) => {
    return (
        <>
            <LottieBase img={'lottieCanasta'} />
        </>
    );
};
