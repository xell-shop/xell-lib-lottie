import React from 'react';
import { LottieBase } from '../Base';

export interface LottieVendedoresProps {}
export const LottieVendedores = ({}: LottieVendedoresProps) => {
    return (
        <>
            <LottieBase img={'lottieVendedores'} />
        </>
    );
};
